# Настройки логирования
LOGGING__LOG_FORMAT=pretty
LOGGING__LOG_FILE=./logs/test.log
LOGGING__LEVEL=DEBUG

# Настройки JWT
TOKEN_SECRET_KEY=test_secret_key
COOKIE_DOMAIN=localhost

# SMTP (для тестов можно использовать mock)
SMTP_PORT=587
SMTP_USERNAME=test_admin
SMTP_PASSWORD=test_admin

# Настройки Redis (для тестов)
REDIS_PORT=6379
REDIS_PASSWORD=default

# Настройки тестовой базы данных
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5433
# POSTGRES_DB=gidrator_db

POSTGRES_USER=godrator
POSTGRES_PASSWORD=0y9o3cyykp97lasy
POSTGRES_HOST=store-database-trpp11
POSTGRES_PORT=5432
POSTGRES_DB=gidrator-db

# Настройки pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

# RabbitMQ
RABBITMQ_USER=guest
RABBITMQ_PASS=guest
RABBITMQ_PORT=5672
RABBITMQ_UI_PORT=15672
RABBITMQ_HOST=localhost
RABBITMQ_EXCHANGE=gidrator_test

# AWS S3 (тестовые настройки - пока не заполнено)
AWS_SERVICE_NAME=s3
AWS_REGION=ru-central1
AWS_ENDPOINT=https://storage.yandexcloud.net
AWS_BUCKET_NAME=test_bucket_name
AWS_ACCESS_KEY_ID=test_access_key_id
AWS_SECRET_ACCESS_KEY=test_secret_access_key

# Настройки CORS
ALLOW_ORIGINS=["*"]
ALLOW_CREDENTIALS=true
ALLOW_METHODS=["*"]
ALLOW_HEADERS=["*"]
